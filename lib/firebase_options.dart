// ATTENZIONE: questo e' un SEGNAPOSTO.
// Sostituisci questo file con quello vero generato da:
//   flutterfire configure
// (vedi README_UNIONE.md, punto 1)
//
// Il file vero conterra' una classe DefaultFirebaseOptions con le
// chiavi reali del vostro progetto Firebase per ogni piattaforma
// (android, ios, web, ...).

import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'DefaultFirebaseOptions non è ancora configurato. '
      'Esegui "flutterfire configure" per generare questo file, '
      'vedi README_UNIONE.md',
    );
  }
}
